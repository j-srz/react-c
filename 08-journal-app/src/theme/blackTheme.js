import { createTheme } from "@mui/material";

export const blackTheme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#025a2f',
    },
    secondary: {
      main: '#021c1c',
    },
  },
});
